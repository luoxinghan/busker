import {keyframes} from "styled-components";

export const keyFrameRun = keyframes`
    from{     
      -webkit-transform:rotate(0deg);     
    }     
    to{     
      -webkit-transform:rotate(360deg);     
    }
`;